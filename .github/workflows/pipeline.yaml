name: CI/CD - Gerenciador Transações

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  # --- JOB DE TESTES ---
  laravel-tests:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: 🔨 Build containers
        run: docker compose -f docker-compose.ci.yml build --no-cache

      - name: 🚀 Subir containers
        run: docker compose -f docker-compose.ci.yml up -d

      - name: 📄 Configurar Permissões e .env
        run: |
          cp backend/.env.example backend/.env
          docker exec -u root laravel-app chmod 777 /var/www/html/.env
          docker exec -u root laravel-app chmod -R 777 /var/www/html/storage /var/www/html/bootstrap/cache

      - name: ⏳ Esperar MySQL ficar pronto
        run: |
          until docker exec laravel-db mysqladmin ping -h"localhost" --silent; do
            sleep 2
          done

      - name: 📦 Instalar dependências
        run: docker exec -u root laravel-app composer install --no-interaction --prefer-dist --no-scripts

      - name: 🔐 Gerar APP_KEY
        run: docker exec laravel-app php artisan key:generate
      
      - name: 🛠 Otimizar e Discover
        run: |
          docker exec laravel-app php artisan package:discover --ansi
          docker exec laravel-app php artisan config:clear

      - name: 🛠 Rodar migrations + seed
        run: docker exec laravel-app php artisan migrate --seed

      - name: 🧪 Rodar testes com coverage
        run: docker exec laravel-app php artisan test --coverage

      - name: 🛑 Derrubar containers 
        if: always() # Garante que roda mesmo se algum passo falhar
        run: docker compose -f docker-compose.ci.yml down

  # --- JOB DE DEPLOY ---
  deploy:
    runs-on: ubuntu-latest
    needs: laravel-tests # SÓ RODA SE O JOB ACIMA PASSAR
    if: github.event_name == 'push' && github.ref == 'refs/heads/master' # Só faz deploy no push da master

    steps:
      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script: |
            # Entra na pasta do projeto no servidor
            cd /var/www/gerenciador-transacoes 
            
            # Atualiza o código
            git pull origin master
            
            # Sobe/Atualiza os containers de produção
            # Nota: No servidor usamos -d para manter rodando
            docker compose -f docker-compose.prod.yml up -d --build
            
            # Comandos de limpeza e otimização pós-deploy
            docker exec laravel-app composer install --no-dev --optimize-autoloader
            docker exec laravel-app php artisan migrate --force
            docker exec laravel-app php artisan config:cache
            docker exec laravel-app php artisan route:cache
            docker exec laravel-app php artisan view:cache